export interface League {
  // チーム名
  team: string;
  // 勝ち点
  points: string;
  // 勝
  wins: string;
  // 負
  losses: string;
  // 分
  draws: string;
  // 得点数
  goalsFor: string;
  // 失点数
  goalsAgainst: string;
  // 得失点
  goalDifference: string;
}
